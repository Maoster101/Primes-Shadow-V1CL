"""Allow running the MCP server with: python -m src.mcp_server"""
# This file exists so `python -m src` doesn't error.
# The MCP server has its own __main__ block — run it with:
#   python -m src.mcp_server
